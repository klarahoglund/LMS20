@using LMS20.Core.Entities
@using lms20.web.services
@inject ICourseService courseService


@{
    var cssClass = Context.Request.Path.ToString().Contains("Dashboard") ? "active" : "";

}
@*
        @*Styrningsnavbar*@
      
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm nav-color border-bottom box-shadow mb-3">
             
            <div class="container-fluid ">



        <div class="title title-spacing text-white">@await courseService.getCourseName(User) </div>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
        <div  class=" navbar-collapse collapse d-sm-inline-flex justify-content-between">
            <ul id="myNavbar"  class="navbar-nav flex-grow-1">
                <li class="nav-item  @(Context.Request.Path.ToString().Contains("Dashboard") ? "active" : "")">

                    <a class="nav-link text-white " asp-area="" asp-controller="Home" asp-action="Dashboard"><i class="central  bi bi-speedometer2"></i><div class="central-text">Dashboard</div></a>
                        </li>
                <li class="nav-item @(Context.Request.Path.ToString().Contains("Modules") ? "active" : "") ">
                    <a class="nav-link text-white" asp-area="" asp-controller="Courses" asp-action="Modules" asp-route-id="@await courseService.getCourseId(User)"><i class=" central2 bi bi-x-diamond-fill"></i><div class="central-text">Översikt</div></a>
                        </li>
                <li class="nav-item @(Context.Request.Path.ToString().Contains("Participants") ? "active" : "")">
                    <a class="nav-link text-white" asp-area="" asp-controller="Courses" asp-action="Participants" asp-route-id="@await courseService.getCourseId(User)"><i class="central bi bi-person-fill"></i><div class="central-text">Studenter</div></a>
                        </li>
                    </ul>
                    
                </div>
            </div>
        </nav>

<script src="~/js/site.js" asp-append-version="true"></script>
